package com.android.common.cameradevice;

import android.media.MediaRecorder;

public interface C0083a {
    void EU(MediaRecorder mediaRecorder);

    void EV(MediaRecorder mediaRecorder, int i);

    void EW(MediaRecorder mediaRecorder, int i, boolean z);
}
